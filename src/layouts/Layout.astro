---
import { ClientRouter } from 'astro:transitions';
import SearchBox from '../components/SearchBox.astro';
import Theme from '../components/Theme.astro';
import Analytics from '@vercel/analytics/astro'

interface Props {
  title?: string;
  description?: string;
}

const { title = 'KeGoLog', description = 'KEGO NOTES - Record learning and life' } = Astro.props;
---

<!doctype html> 
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <ClientRouter fallback="swap" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <title>{title}</title>
    <script is:inline>
      // 立即应用主题，避免闪烁
      (function() {
        const themes = {
          'ponkotsu-house': {
            bg: '#F2F2F2',
            border: '#D9D4CC',
            textPrimary: '#59382C',
            textSecondary: '#8C5B49',
          },
          'midnight': {
            bg: '#1A1B26',
            border: '#2D3142',
            textPrimary: '#C0CAF5',
            textSecondary: '#7AA2F7',
          },
          'forest': {
            bg: '#F0F4ED',
            border: '#C9D6C4',
            textPrimary: '#2F5233',
            textSecondary: '#4A7C4E',
          },
          'ocean': {
            bg: '#E8F2F7',
            border: '#B8D8E8',
            textPrimary: '#0D4F6F',
            textSecondary: '#2B7A9E',
          },
        };

        function applyTheme() {
          const savedThemeId = localStorage.getItem('theme-id') || 'ponkotsu-house';
          const theme = themes[savedThemeId] || themes['ponkotsu-house'];

          if (theme) {
            const root = document.documentElement;
            root.style.setProperty('--color-bg', theme.bg);
            root.style.setProperty('--color-border', theme.border);
            root.style.setProperty('--color-text-primary', theme.textPrimary);
            root.style.setProperty('--color-text-secondary', theme.textSecondary);
          }
        }

        // 初次加载时应用主题
        applyTheme();

        // 监听 Astro 路由切换事件，在页面交换前应用主题
        document.addEventListener('astro:before-swap', applyTheme);
        document.addEventListener('astro:after-swap', applyTheme);
      })();
    </script>
  </head>
  <body>
    <header class="header">
      <div class="header-container">
        <a href="/" class="header-logo">
          <span class="logo-icon"></span>
          <span class="logo-text">席剑豪</span>
        </a>
        <SearchBox />
        <div class="header-right">
          <nav class="social-links">
            <a 
              href="https://keepgoing.top" 
              target="_blank" 
              rel="noopener noreferrer"
              class="social-link"
              aria-label="KeepGoing"
            >
              <svg class="social-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
                <polyline points="9 22 9 12 15 12 15 22"/>
              </svg>
            </a>
            <a 
              href="https://github.com/xijianhao" 
              target="_blank" 
              rel="noopener noreferrer"
              class="social-link"
              aria-label="GitHub"
            >
              <svg class="social-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"></path>
              </svg>
             
            </a>
            
            <!-- <a href="#" class="social-link" aria-label="小程序">小程序图标</a>
            <a href="#" class="social-link" aria-label="小红书">小红书图标</a> -->
          </nav>
          <Theme />
        </div>
      </div>
    </header>
    <main class="main">
      <slot />
      <Analytics />
    </main>
    <footer class="footer">
      <p>© 2025 KeGoLog - Record learning and life</p>
    </footer>
  </body>
</html>

<style is:global>
  :root {
    /* 主题颜色变量 */
    --color-bg: #F2F2F2;
    --color-border: #D9D4CC;
    --color-text-primary: #59382C;
    --color-text-secondary: #8C5B49;
  }

  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  html {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
      'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
      sans-serif;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    width: 100%;
    height: 100%;
    overflow: hidden;
  }

  body {
    background: var(--color-bg);
    height: 100vh;
    color: var(--color-text-primary);
    line-height: 1.6;
    width: 100%;
    margin: 0;
    padding: 0;
    overflow: hidden;
    display: flex;
    flex-direction: column;
  }

  .header {
    background: var(--color-border);
    border-bottom: 1px solid var(--color-border);
    position: relative;
    z-index: 100;
    box-shadow: 0 1px 3px rgba(89, 56, 44, 0.1);
    width: 100%;
    flex-shrink: 0;
  }

  .header-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0.75rem 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    width: 100%;
    box-sizing: border-box;
  }

  .header-right {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-left: auto;
  }

  .social-links {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .social-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    color: var(--color-text-primary);
    text-decoration: none;
    border-radius: 0.375rem;
    transition: all 0.2s ease;
  }

  .social-link:hover {
    background: rgba(89, 56, 44, 0.1);
    transform: translateY(-1px);
  }

  .social-icon {
    width: 1.25rem;
    height: 1.25rem;
  }

  .header-logo {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    text-decoration: none;
    color: var(--color-text-primary);
    font-size: 1.125rem;
    font-weight: 600;
    flex-shrink: 0;
  }

  .logo-icon {
    font-size: 1.375rem;
  }

  .main {
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem;
    width: 100%;
    box-sizing: border-box;
    flex: 1;
    overflow:auto;
  }

  .footer {
    text-align: center;
    padding: 1rem;
    color: var(--color-text-secondary);
    font-size: 0.875rem;
    width: 100%;
    box-sizing: border-box;
    flex-shrink: 0;
  }

  @media (max-width: 768px) {
    html {
      height: 100%;
      overflow: hidden;
    }

    body {
      height: 100vh;
      height: 100dvh; /* 使用动态视口高度，避免移动端浏览器地址栏影响 */
      overflow: hidden;
      display: flex;
      flex-direction: column;
    }

    .header {
      position: relative;
      flex-shrink: 0;
      z-index: 1000;
    }

    .header-container {
      padding: 0.75rem 0.5rem;
      gap: 0.375rem;
      flex-wrap: nowrap;
      overflow: hidden;
    }

    .header-logo {
      font-size: 1rem;
      gap: 0.5rem;
      flex-shrink: 0;
    }

    .logo-icon {
      font-size: 1.125rem;
    }

    .header-right {
      gap: 0.375rem;
      margin-left: auto;
      flex-shrink: 0;
    }

    .social-links {
      gap: 0.25rem;
    }

    .social-link {
      width: 1.75rem;
      height: 1.75rem;
    }

    .social-icon {
      width: 1.125rem;
      height: 1.125rem;
    }

    .main {
      padding: 1rem;
      width: 100%;
      max-width: 100%;
      flex: 1;
      overflow-y: auto;
      overflow-x: hidden;
      -webkit-overflow-scrolling: touch;
    }

    .footer {
      padding: 0.5rem 1rem;
      font-size: 0.75rem;
      width: 100%;
      flex-shrink: 0;
    }
  }
</style>
