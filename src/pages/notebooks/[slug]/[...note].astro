---
import Layout from '../../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { getNotebookBySlug } from '../../../utils/notebooks';

export async function getStaticPaths() {
  const notes = await getCollection('notes');
  
  return notes.map((note) => {
    const notebookSlug = note.data.notebook;
    // note.slug 可能包含路径，如 "react/react-hooks"
    // [...note] 路由会自动处理路径，直接传递字符串即可
    return {
      params: { slug: notebookSlug, note: note.slug },
      props: { note, notebookSlug },
    };
  });
}

// 直接使用 props 中传递的 note，避免重新查找
const { note, notebookSlug } = Astro.props;

const notebook = await getNotebookBySlug(notebookSlug);
const { Content } = await note.render();
const { title, date, tags } = note.data;
---

<Layout title={`${title} - ${notebook?.data.title || '知识库'}`}>
  <article class="note-page">
    <nav class="note-nav">
      <a href={`/notebooks/${notebookSlug}`} class="nav-link">
        ← 返回 {notebook?.data.title || '笔记本'}
      </a>
    </nav>

    <header class="note-header">
      <h1 class="note-title">{title}</h1>
      <div class="note-meta">
        <time class="note-date" datetime={date.toISOString()}>
          {date.toLocaleDateString('zh-CN', { 
            year: 'numeric', 
            month: 'long', 
            day: 'numeric' 
          })}
        </time>
        {tags && tags.length > 0 && (
          <div class="note-tags">
            {tags.map(tag => (
              <span class="note-tag">{tag}</span>
            ))}
          </div>
        )}
      </div>
    </header>

    <div class="note-content">
      <Content />
    </div>
  </article>
</Layout>

<style>
  .note-page {
    animation: fadeIn 0.5s ease-in;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .note-nav {
    margin-bottom: 2rem;
  }

  .nav-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-text-primary);
    text-decoration: none;
    font-size: 0.875rem;
    margin-bottom: 1.5rem;
    padding: 0.5rem 1rem;
    background: var(--color-bg);
    border-radius: 8px;
    border: 1px solid var(--color-border);
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    font-weight: 500;
    box-shadow: 0 1px 3px rgba(89, 56, 44, 0.1);
  }

  .nav-link:hover {
    background: var(--color-border);
    border-color: var(--color-text-secondary);
    transform: translateX(-3px);
    box-shadow: 0 2px 6px rgba(89, 56, 44, 0.2);
    color: var(--color-text-primary);
  }

  .nav-link:active {
    transform: translateX(-3px) scale(0.98);
  }

  .note-header {
    background: var(--color-bg);
    padding: 1.75rem;
    border-radius: 16px;
    margin-bottom: 2rem;
    box-shadow: 0 4px 12px rgba(89, 56, 44, 0.15);
    border: 1px solid var(--color-border);
  }

  .note-title {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: var(--color-text-primary);
    line-height: 1.3;
  }

  .note-meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 1rem;
  }

  .note-date {
    font-size: 0.8125rem;
    color: var(--color-text-secondary);
  }

  .note-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .note-tag {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    font-size: 0.75rem;
    background: var(--color-border);
    color: var(--color-text-primary);
    border-radius: 12px;
    font-weight: 500;
    border: 1px solid var(--color-border);
  }

  .note-content {
    background: var(--color-bg);
    padding: 2rem;
    border-radius: 16px;
    box-shadow: 0 4px 6px rgba(89, 56, 44, 0.1);
    line-height: 1.8;
    color: var(--color-text-primary);
    border: 1px solid var(--color-border);
  }

  .note-content :global(h1),
  .note-content :global(h2),
  .note-content :global(h3),
  .note-content :global(h4),
  .note-content :global(h5),
  .note-content :global(h6) {
    margin-top: 2rem;
    margin-bottom: 1rem;
    font-weight: 600;
    color: var(--color-text-primary);
    line-height: 1.3;
  }

  .note-content :global(h1) {
    font-size: 2rem;
    border-bottom: 2px solid var(--color-border);
    padding-bottom: 0.5rem;
  }

  .note-content :global(h2) {
    font-size: 1.5rem;
  }

  .note-content :global(h3) {
    font-size: 1.25rem;
  }

  .note-content :global(p) {
    margin-bottom: 1rem;
  }

  .note-content :global(ul),
  .note-content :global(ol) {
    margin-bottom: 1rem;
    padding-left: 2rem;
  }

  .note-content :global(li) {
    margin-bottom: 0.5rem;
  }

  .note-content :global(code) {
    background: var(--color-border);
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
    font-size: 0.9em;
    font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
    color: var(--color-text-primary);
  }

  .note-content :global(pre) {
    background: var(--color-text-primary);
    color: var(--color-bg);
    padding: 1.5rem;
    border-radius: 8px;
    overflow-x: auto;
    margin-bottom: 1.5rem;
    line-height: 1.6;
    border: 1px solid var(--color-text-secondary);
  }

  .note-content :global(pre code) {
    background: transparent;
    padding: 0;
    color: inherit;
    font-size: 0.875rem;
  }

  .note-content :global(blockquote) {
    border-left: 4px solid var(--color-text-secondary);
    padding-left: 1rem;
    margin-left: 0;
    margin-bottom: 1rem;
    color: var(--color-text-secondary);
    font-style: italic;
    background: var(--color-border);
    padding: 1rem;
    border-radius: 4px;
  }

  .note-content :global(a) {
    color: var(--color-text-secondary);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 0.2s;
  }

  .note-content :global(a:hover) {
    border-bottom-color: var(--color-text-secondary);
    color: var(--color-text-primary);
  }

  .note-content :global(img) {
    max-width: 100%;
    height: auto;
    border-radius: 8px;
    margin: 1.5rem 0;
  }

  .note-content :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 1.5rem;
  }

  .note-content :global(th),
  .note-content :global(td) {
    padding: 0.75rem;
    text-align: left;
    border-bottom: 1px solid var(--color-border);
  }

  .note-content :global(th) {
    font-weight: 600;
    background: var(--color-border);
    color: var(--color-text-primary);
  }

  @media (max-width: 768px) {
    .note-header {
      padding: 1.5rem;
    }

    .note-title {
      font-size: 1.75rem;
    }

    .note-content {
      padding: 1.5rem;
    }

    .note-content :global(h1) {
      font-size: 1.5rem;
    }

    .note-content :global(h2) {
      font-size: 1.25rem;
    }

    .note-content :global(pre) {
      padding: 1rem;
      font-size: 0.8125rem;
    }
  }
</style>

